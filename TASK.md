可以。你现在已经把所有原始题目和附件都放进 Codex 工作区了，所以这份任务书应该明确告诉 Codex：

**先完整阅读题目和附件，再建模、再编码；原始文档是最高优先级依据，不要根据提示词自行猜公式。**

尤其这道题的 Word 里有公式，普通文本抽取有可能丢掉数学公式，所以最好专门要求 Codex：**如果常规 DOCX 解析无法读出公式，需要检查 DOCX 内部 XML/Office Math 内容或采用其他方法恢复公式，不能凭经验补公式。**

下面这份我建议你可以**直接整体复制给 Codex**。

---

# 《山区洪涝灾害下无人机运输与通信协同优化》完整开发任务书

## 1. 项目目标

请在当前工作区中，完成数学建模题：

**《山区洪涝灾害下无人机运输与通信协同优化》**

的完整数据处理、数学计算、优化求解、结果验证、可视化和结果导出程序。

项目需要覆盖：

$$
\boxed{\text{问题一、问题二、问题三、问题四}}
$$

并建立一套能够重复运行、方便修改参数、方便检查结果的 Python 工程。

不要只写一个能够“跑出答案”的脚本。需要把：

$$
\text{数据读取}
\rightarrow
\text{地理计算}
\rightarrow
\text{飞行模型}
\rightarrow
\text{能耗模型}
\rightarrow
\text{优化模型}
\rightarrow
\text{结果验证}
\rightarrow
\text{结果输出}
$$

拆成清晰模块。

同时必须生成：

```text
README.md
```

用于完整说明项目结构、环境、数据、运行方式、算法和结果文件。

---

# 2. 工作区与原始数据

当前工作区根目录为：

```text
F:\2026 math D
```

但代码中**禁止硬编码绝对路径**。

必须以项目根目录为基准，通过 `pathlib.Path` 等方式使用相对路径，并保证中文目录和中文文件名可以正常读取。

工作区结构如下：

```text
F:\2026 math D
│
├─ 山区洪涝灾害下无人机运输与通信协同优化.docx
├─ 结果提交模板.xlsx
│
└─ 数据
   │
   ├─ 无人机应急物资运输基础数据
   │  ├─ 中继无人机数据.xlsx
   │  ├─ 物资需求与配送时限.xlsx
   │  ├─ 调度中心与服务区.xlsx
   │  ├─ 运输无人机数据.xlsx
   │  └─ 通信链路参数.xlsx
   │
   └─ 镇龙乡地理空间数据
      ├─ 镇龙乡地理空间数据说明.pdf
      ├─ 镇龙乡地理空间详情地图.html
      │
      └─ 镇龙乡及周边地理数据
         ├─ 数字高程模型数据（DEM）
         │  ├─ 镇龙乡及周边30米DEM.mat
         │  └─ 镇龙乡及周边30米DEM.tif
         │
         ├─ 村镇点位
         │  ├─ 镇龙乡及周边村镇点位.csv
         │  └─ 镇龙乡及周边村镇点位.mat
         │
         ├─ 水体（面）
         │  ├─ 镇龙乡及周边水体.csv
         │  └─ 镇龙乡及周边水体.mat
         │
         ├─ 水系（线）
         │  ├─ 镇龙乡及周边水系.csv
         │  └─ 镇龙乡及周边水系.mat
         │
         └─ 道路
            ├─ 镇龙乡及周边道路.csv
            └─ 镇龙乡及周边道路.mat
```

---

# 3. 原始文件是最高优先级依据

在开始编程前，必须首先阅读：

```text
山区洪涝灾害下无人机运输与通信协同优化.docx
```

以及所有与当前问题相关的 Excel、PDF、DEM 和地理数据。

**题目原文和附件参数是唯一正式计算依据。**

不要依据本任务书自行补充题目没有规定的物理规则。

如果本任务书和原题出现冲突：

$$
\boxed{\text{始终以原始题目和附件为准}}
$$

尤其注意 Word 文档中的数学公式。

如果常规 `python-docx` 文本提取时公式缺失，必须进一步检查 DOCX 内部 XML、Office Math、公式对象或采用其他方法获得准确公式。

**禁止因为公式抽取失败，就自行假定线性公式、经验公式或常用公式。**

遇到无法确认的内容，应：

```text
1. 在日志中明确标记；
2. 在 README 中说明；
3. 尽可能从原题或附件中恢复；
4. 不要静默自行补充假设。
```

---

# 4. 第一阶段：先做数据与题意审计，不要立即写优化器

正式求解前，请先自动完成一次数据审计。

需要读取所有 Excel 的：

```text
sheet 名称
字段名称
数据类型
单位
行数
缺失值
ID 编号
重复项
异常值
```

并检查：

```text
服务区数量
货箱数量
运输无人机型号
实体无人机数量
共享电池数量
中继无人机数量
中继能源组件数量
通信参数完整性
DEM CRS
DEM 分辨率
DEM NoData
DEM 经纬度范围
```

生成：

```text
outputs/data_audit/
```

至少包含：

```text
data_summary.xlsx
data_schema.json
data_validation.txt
```

如果数据存在异常，不要直接忽略。

---

# 5. 建立统一的基础计算模块

四个问题必须使用**同一套基础物理计算函数**，避免各问计算口径不一致。

## 5.1 地理距离

根据题目坐标系统计算两个任务节点之间的水平距离。

必须考虑经纬度不是平面坐标这一事实。

选择合理的球面距离或局部投影计算方式，并在 README 中说明。

不要简单把：

```text
经度差、纬度差
```

直接当作米。

---

## 5.2 DEM 航段分析

任意两个任务节点 \(i,j\) 之间：

$$
i\rightarrow j
$$

均按照题目规定的水平直线路径。

需要沿航段读取 DEM，得到：

$$
H_{ij}^{max}
=
\max(\text{航段经过的DEM像元高程})
$$

根据原题计算计划巡航海拔。

DEM GeoTIFF 应作为主要计算数据源。

要求：

```text
正确处理 EPSG
正确处理 NoData
正确处理像元索引
正确处理边界
保证线段采样不会漏掉最高地形
```

最好预计算所有任务节点两两之间的：

```text
水平距离
最高DEM
巡航海拔
起点爬升高度
终点下降高度
```

形成缓存矩阵，避免优化过程中重复读取 DEM。

---

## 5.3 作业高度

严格按照原题定义调度中心和服务区的作业高度。

对于多点运输路线：

$$
O01\rightarrow S_i\rightarrow S_j\rightarrow O01
$$

每个航段都必须单独计算：

$$
O01\rightarrow S_i
$$

$$
S_i\rightarrow S_j
$$

$$
S_j\rightarrow O01
$$

的 DEM、巡航海拔、爬升和下降。

不能给整个架次使用一个统一巡航高度，除非原题明确如此规定。

---

## 5.4 运输无人机等效航程

根据原题给出的：

```text
空载标准航程
满载标准航程
最大载质量
当前剩余载荷
```

严格实现等效航程公式。

不得自行推断公式。

多点配送过程中，随着货物逐站卸下，当前载荷发生变化：

$$
q_0>q_1>q_2>\cdots
$$

所以每个航段必须使用该航段实际剩余有效载荷计算。

---

## 5.5 飞行时间

每个航段需要分别计算：

$$
t_{ij}
=
t^{climb}_{ij}
+
t^{cruise}_{ij}
+
t^{descent}_{ij}
$$

并使用原题规定的：

```text
爬升速度
巡航速度
下降速度
```

必要时加入题目附件规定的：

```text
准备时间
投送作业时间
架次周转时间
建链时间
```

不得遗漏。

---

## 5.6 运输能耗

按照原题公式计算：

$$
E_{ij}
$$

必须正确区分：

```text
水平巡航能耗
爬升附加能耗
下降能耗
```

是否计算下降附加能耗必须严格按照题面规定。

一个完整运输架次：

$$
r=(O01,S_{i_1},S_{i_2},...,O01)
$$

总能耗：

$$
E_r=\sum E_{ij}
$$

每段根据当时剩余载荷重新计算。

---

## 5.7 返航安全余量

所有运输架次和中继架次必须满足题目规定的返航安全能量要求。

要求实现通用函数，例如：

```python
check_energy_feasibility(...)
```

输出：

```text
总能耗
允许最大能耗
剩余SOC
返航安全余量
是否可行
```

---

# 6. 问题一：单点往返运输能力与货箱组批

问题一不考虑：

```text
具体实体无人机
共享电池资源竞争
架次具体开始时间
并行调度
中继通信
```

每个架次必须：

$$
O01\rightarrow S_i\rightarrow O01
$$

且只能服务一个服务区。

同一个服务区允许多个架次。

不同架次允许使用不同运输机型。

不能跨服务区组批。

---

## 6.1 最大安全载荷

对：

```text
3 种运输无人机型号
×
15 个服务区
```

计算所有：

$$
Q_{k,i}^{safe}
$$

即：

$$
\boxed{\text{机型 }k\text{ 前往服务区 }i\text{ 的最大安全载荷}}
$$

需要同时满足：

```text
结构最大载重
飞行能量
返航安全余量
```

建议通过解析、二分搜索或高精度搜索得到。

结果输出为：

```text
q1_safe_payload_matrix.xlsx
```

---

## 6.2 货箱组批

货箱：

```text
不可拆分
不能重复
不能遗漏
不得跨服务区组批
```

每个批次必须同时满足：

$$
\sum w_j\leq Q_{k,i}^{safe}
$$

以及：

$$
\sum v_j\leq V_k^{max}
$$

并再次通过完整架次能耗校验。

决策对象包括：

```text
某批包含哪些货箱
该批使用哪种机型
```

---

## 6.3 Q1优化指标

综合考虑：

$$
\boxed{\text{总往返架次数}}
$$

$$
\boxed{\text{总运输能耗}}
$$

$$
\boxed{\text{累计作业时间}}
$$

不要擅自固定一组任意权重。

程序至少支持：

```text
字典序优化
归一化加权多目标
候选 Pareto 解比较
```

权重和目标优先级必须放到配置文件中。

---

## 6.4 安全余量敏感性分析

改变返航安全余量参数，重新计算：

```text
最大安全载荷
组批结果
架次数
总能耗
累计时间
```

输出敏感性曲线和数据表。

---

# 7. 问题二：异构无人机多点多架次运输调度

问题二开始考虑真正的实体运输调度。

每一个架次允许：

$$
O01
\rightarrow
S_{i_1}
\rightarrow
S_{i_2}
\rightarrow
\cdots
\rightarrow
O01
$$

联合决定：

$$
\boxed{
\text{货箱组批}
+
\text{访问顺序}
+
\text{机型}
+
\text{实体无人机}
+
\text{共享电池}
+
\text{架次开始时间}
}
$$

---

## 7.1 Q2约束

至少严格实现：

```text
货箱不可拆
每箱恰好配送一次
载重量
装载体积
运输能量
返航安全余量
实体无人机数量
同一无人机任务不重叠
共享电池数量
电池机型匹配
电池任务占用不重叠
电池充电过程不重叠
电池再次使用前满足题目规定SOC要求
物资配送时限
架次必须返回O01
```

---

## 7.2 物资时间要求

必须直接读取：

```text
物资需求与配送时限.xlsx
```

并严格区分：

```text
医疗物资期望送达时间
首批必须送达箱数
首批截止时间
普通物资期望送达时间
应急优先系数
```

根据原题定义确定哪些是硬约束，哪些进入配送及时性评价。

不得仅按照“应急优先系数最大优先”进行简单排序。

---

## 7.3 电池调度

共享电池是独立资源。

必须记录每块电池：

```text
battery_id
所属机型
占用时间
任务结束SOC
开始充电时间
结束充电时间
再次使用时间
```

严格按照题目规定的两阶段充电模型计算。

---

## 7.4 Q2优化目标

至少包括：

$$
\boxed{\text{配送及时性}}
$$

$$
\boxed{\text{全部任务完成时间 Makespan}}
$$

$$
\boxed{\text{总运输能耗}}
$$

$$
\boxed{\text{运输架次数}}
$$

其中：

$$
T_{\max}
=
\max(\text{所有运输无人机最后返回O01的时刻})
$$

---

# 8. 问题三：通信约束下运输与中继联合调度

问题三继承问题二全部运输约束。

新增：

$$
\boxed{\text{连续通信约束}}
$$

运输无人机在：

```text
爬升
巡航
下降
物资投送
```

期间均必须保持通信。

---

# 9. 固定网关与直连通信

首先判断运输无人机与：

```text
G01
```

是否可以直接通信。

不得仅用二维距离判断。

必须使用题目规定的：

```text
三维距离
地形视距
DEM遮挡
自由空间传播损耗
遮挡附加损耗
发射功率
发射天线增益
接收天线增益
系统损耗
接收灵敏度
衰落裕量
双向链路预算
```

实现完整链路判定函数。

---

# 10. 中继通信

如果：

$$
Transport\leftrightarrow G01
$$

直连不可用，则允许：

$$
Transport
\leftrightarrow
Relay
\leftrightarrow
G01
$$

但必须同时满足：

$$
C_{T,R}(t)=1
$$

和：

$$
C_{R,G01}(t)=1
$$

并且不允许：

$$
T\rightarrow R_1\rightarrow R_2\rightarrow G01
$$

这类多级中继。

---

# 11. 中继无人机决策变量

需要优化：

$$
\boxed{
\text{具体中继无人机}
}
$$

$$
\boxed{
\text{悬停经纬度}
}
$$

$$
\boxed{
\text{悬停高度}
}
$$

$$
\boxed{
\text{开始服务时间}
}
$$

$$
\boxed{
\text{结束服务时间}
}
$$

$$
\boxed{
\text{能源组件}
}
$$

悬停位置必须满足 DEM 范围和原题高度限制。

一个中继架次中，默认采用：

$$
O01\rightarrow P\rightarrow\text{固定悬停}\rightarrow O01
$$

除非原题明确支持移动中继，否则不要自行引入“边飞边中继”。

---

# 12. 多运输无人机共享中继

不要默认：

```text
1架运输无人机 = 1架中继无人机
```

如果题目附件没有明确给出并发容量限制，则允许：

$$
\boxed{\text{多个运输无人机在同一时刻共享同一架中继无人机}}
$$

条件是：

每一架运输无人机与该中继之间的链路均满足要求，并且：

$$
Relay\leftrightarrow G01
$$

回传链路同时有效。

如果原始通信参数中存在：

```text
最大并发连接数
带宽
容量
用户上限
```

则必须按原始附件约束执行。

---

# 13. 中继自身时间与能量

中继无人机不是静态基站。

必须计算：

$$
\text{准备}
\rightarrow
\text{飞往悬停点}
\rightarrow
\text{建链}
\rightarrow
\text{悬停通信}
\rightarrow
\text{返回O01}
$$

总能耗包含题面要求的：

$$
E_{relay}
=
E_{flight}
+
E_{hover}
+
E_{communication}
$$

并满足返航安全余量。

同时需要调度：

```text
中继无人机实体
中继能源组件
充电
周转
```

---

# 14. Q3优化目标

优化：

$$
\boxed{\text{物资配送及时性}}
$$

$$
\boxed{\text{联合任务完成时间}}
$$

$$
\boxed{\text{运输 + 中继总能耗}}
$$

$$
\boxed{\text{运输无人机 + 中继无人机使用架次}}
$$

其中联合 Makespan 为：

$$
T_{\max}^{joint}
=
\max(
\text{所有运输机和中继机最终返回O01时刻}
)
$$

最终必须保证：

$$
\boxed{\text{所有运输任务期间通信中断时间}=0}
$$

---

# 15. 问题四：任务分区与资源配置

问题四必须以：

$$
\boxed{\text{问题三最终联合调度方案}}
$$

为输入。

不能重新修改问题三已经确定的：

```text
货箱组批
服务区访问顺序
运输任务
中继任务
通信保障关系
```

需要分别求两种情况。

第一种：

$$
15\text{个服务区}\rightarrow2\text{个任务组}
$$

第二种：

$$
15\text{个服务区}\rightarrow3\text{个任务组}
$$

两种都必须求。

---

## 15.1 分区约束

每个服务区：

$$
\boxed{\text{恰好属于一个任务组}}
$$

每个任务组：

$$
\boxed{\text{至少包含一个服务区}}
$$

如果问题三中某个运输架次：

$$
O01\rightarrow S001\rightarrow S004\rightarrow S007\rightarrow O01
$$

则：

$$
S001,S004,S007
$$

必须属于同一个任务组。

不能把同一架次拆开。

---

## 15.2 独立资源约束

各任务组独立执行。

以下资源不能跨组动态共享：

```text
运输无人机
共享电池
中继无人机
中继能源组件
```

因此每组需要重新计算独立完成本组任务所需的最小资源规模。

---

## 15.3 Q4比较指标

比较：

$$
\boxed{\text{资源配置规模}}
$$

$$
\boxed{\text{资源冗余}}
$$

$$
\boxed{\text{组间工作量均衡}}
$$

$$
\boxed{\text{现有库存资源缺口}}
$$

如果资源超过库存：

```text
明确指出缺少哪种资源
缺少数量
出现缺口的任务组
导致缺口的时间段/任务原因
```

---

# 16. 优化算法要求

不要一开始就强制将四问全部写成一个超大型 MILP。

先评估问题规模。

可以考虑分层架构：

$$
\boxed{
\text{基础矩阵预计算}
\rightarrow
\text{候选方案生成}
\rightarrow
\text{优化搜索}
\rightarrow
\text{精确可行性验证}
}
$$

允许根据问题使用：

```text
枚举
动态规划
MILP
CP-SAT
VRP算法
局部搜索
遗传算法
模拟退火
NSGA-II
混合启发式
```

但需要说明为什么选择该算法。

如果采用启发式或元启发式：

```text
必须固定随机种子
保存迭代历史
输出最优值变化曲线
允许配置迭代次数
允许配置时间限制
```

---

# 17. 非常重要：优化器和验证器必须分离

必须编写独立：

```text
validator.py
```

优化器输出结果以后，由 validator 按原始规则重新计算全部约束。

不要直接相信优化器内部结果。

验证内容至少包括：

```text
80个货箱全部配送
每个货箱恰好一次
货箱不可拆
载重不超限
体积不超限
每段载荷正确递减
DEM巡航高度正确
单架次能耗正确
返航SOC合格
无人机时间不冲突
电池时间不冲突
电池充电逻辑正确
物资硬时限满足
中继位置合法
中继能量合法
通信链路双向可用
问题三全程通信连续
问题四所有服务区恰好属于一个组
问题四绑定服务区未被拆分
问题四资源未跨组使用
```

生成：

```text
outputs/validation_report.txt
```

和：

```text
outputs/validation_report.xlsx
```

---

# 18. 建议工程目录

请将项目重构为类似：

```text
F:\2026 math D
│
├─ README.md
├─ requirements.txt
├─ config.yaml
├─ main.py
│
├─ src
│  ├─ __init__.py
│  ├─ paths.py
│  ├─ data_loader.py
│  ├─ data_models.py
│  ├─ geo.py
│  ├─ dem.py
│  ├─ flight.py
│  ├─ transport_energy.py
│  ├─ battery.py
│  ├─ communication.py
│  ├─ relay.py
│  ├─ objectives.py
│  ├─ validator.py
│  │
│  ├─ q1
│  │  ├─ model.py
│  │  ├─ solver.py
│  │  └─ report.py
│  │
│  ├─ q2
│  │  ├─ model.py
│  │  ├─ solver.py
│  │  └─ report.py
│  │
│  ├─ q3
│  │  ├─ model.py
│  │  ├─ solver.py
│  │  └─ report.py
│  │
│  └─ q4
│     ├─ model.py
│     ├─ solver.py
│     └─ report.py
│
├─ outputs
│  ├─ data_audit
│  ├─ q1
│  ├─ q2
│  ├─ q3
│  ├─ q4
│  ├─ figures
│  └─ validation
│
└─ 原始题目及数据文件
```

如果有更合理的架构可以调整，但必须模块化，禁止把全部逻辑堆在一个文件里。

---

# 19. 配置文件

建立：

```text
config.yaml
```

存放可调参数，例如：

```text
随机种子
求解器时间限制
目标函数模式
目标权重
浮点容差
DEM采样设置
敏感性分析范围
启发式算法参数
可视化开关
日志等级
```

题目直接给出的物理参数尽量从附件读取，不要重复硬编码进配置文件。

---

# 20. 输出要求

## Q1

输出：

```text
三机型×15服务区最大安全载荷
每个服务区货箱组批
每架次机型
每架次货箱
重量
体积
能耗
飞行时间
总架次数
总能耗
累计作业时间
安全余量敏感性分析
```

---

## Q2

输出：

```text
运输架次编号
运输机型
实体无人机编号
共享电池编号
开始时间
返回时间
完整路线
每个服务区到达时间
每个货箱实际送达时间
当前载荷变化
各航段能耗
总架次能耗
电池SOC
电池充电时间
```

---

## Q3

在 Q2 基础上继续输出：

```text
直连/中继通信状态
需要中继的时间区间
中继无人机编号
中继能源组件
悬停经度
悬停纬度
悬停地面高程
悬停离地高度
悬停绝对海拔
中继起飞时刻
到达时刻
建链完成时刻
服务开始时刻
服务结束时刻
返航时刻
中继能耗
接入链路裕量
回传链路裕量
```

---

## Q4

输出：

```text
2组分区方案
3组分区方案
每组服务区
每组运输任务
每组中继任务
运输无人机需求
共享电池需求
中继无人机需求
中继能源组件需求
工作量
资源冗余
库存缺口
```

---

# 21. 可视化要求

至少生成：

```text
Q1安全载荷热力图
安全余量敏感性曲线
运输路线地图
无人机调度Gantt图
共享电池占用/充电Gantt图
逐箱送达时间图
中继无人机服务时间轴
通信直连/中继状态图
Q3运输+中继地图
Q4两组分区地图
Q4三组分区地图
资源需求对比图
```

地图应尽可能结合 DEM 或题目提供的地理数据。

---

# 22. 结果提交模板

读取：

```text
结果提交模板.xlsx
```

分析模板中要求填写的工作表和字段。

最终尽可能自动生成：

```text
outputs/结果提交.xlsx
```

不要修改官方模板要求的字段名称和基本结构。

如果部分字段无法自动确定，要明确记录。

---

# 23. README.md 必须完整生成

项目根目录必须生成：

```text
README.md
```

README 至少包含以下内容：

```text
项目简介
题目四问概述
工作区目录结构
原始数据文件说明
Python环境要求
依赖安装方法
如何运行
如何只运行Q1/Q2/Q3/Q4
配置文件说明
数据预处理方法
DEM处理方法
运输飞行模型
能耗模型
电池模型
通信链路模型
问题一算法
问题二算法
问题三算法
问题四算法
多目标处理方式
输出目录说明
主要结果文件说明
可视化说明
验证器说明
已知假设
存在的题意歧义
运行时间或性能说明
常见错误及排查方法
```

建议提供运行示例：

```bash
python main.py --question all
python main.py --question q1
python main.py --question q2
python main.py --question q3
python main.py --question q4
python main.py --validate
```

实际参数可以根据最终程序设计调整，但 README 与代码必须一致。

---

# 24. requirements.txt

生成完整：

```text
requirements.txt
```

优先采用成熟 Python 库。

例如根据实际需要考虑：

```text
numpy
pandas
openpyxl
scipy
rasterio
pyproj
shapely
matplotlib
networkx
ortools
pulp
```

不要为了使用库而使用库，只保留实际依赖。

---

# 25. 日志与中间结果

程序运行时不要只在终端打印最终答案。

建立日志系统，例如：

```text
outputs/logs/
```

记录：

```text
数据读取
DEM预计算
候选路线数量
求解器状态
目标函数
约束违例
优化过程
运行时间
最终验证
```

大量耗时的基础计算应支持缓存。

例如：

```text
node_pair_geometry_cache.pkl
communication_cache.pkl
```

避免重复运行时重新计算全部 DEM 和链路。

---

# 26. 数值精度

建立统一数值容差。

例如：

```python
EPS = ...
```

不要因为浮点误差导致：

```text
载重超限 1e-9
SOC 低于阈值 1e-10
通信损耗相差 1e-8
```

就错误判定不可行。

最终报告值与内部高精度计算分开：

```text
内部计算保持足够精度
输出时统一四舍五入
```

具体保留位数参考官方结果模板。

---

# 27. 开发顺序

不要一上来同时写四问。

推荐严格按：

$$
\boxed{
\text{阶段0：读题与数据审计}
}
$$

↓

$$
\boxed{
\text{阶段1：公共地理/飞行/能耗模块}
}
$$

↓

$$
\boxed{
\text{阶段2：Q1}
}
$$

↓

验证 Q1 基础物理计算

↓

$$
\boxed{
\text{阶段3：Q2}
}
$$

↓

验证无人机、电池、时间调度

↓

$$
\boxed{
\text{阶段4：通信链路模块}
}
$$

↓

单独测试通信链路

↓

$$
\boxed{
\text{阶段5：Q3}
}
$$

↓

验证全程连续通信

↓

$$
\boxed{
\text{阶段6：Q4}
}
$$

↓

$$
\boxed{
\text{阶段7：结果模板、可视化、README}
}
$$

---

# 28. 每个阶段先做小规模人工可验证测试

正式全量优化前，至少建立以下单元测试或人工可验证样例：

### 示例1

```text
O01 → S001 → O01
```

检查：

```text
距离
DEM最高点
巡航高度
爬升
下降
时间
带货去程能耗
空载返程能耗
安全SOC
```

### 示例2

```text
O01 → S001 → S005 → O01
```

检查：

```text
不同航段DEM
载荷递减
各段能耗
总时间
总能耗
```

### 示例3

单架运输无人机沿一条已知路线飞行，逐时刻测试：

```text
Transport ↔ G01
```

通信是否正确。

### 示例4

增加一个固定悬停中继点，检查：

```text
Transport ↔ Relay
Relay ↔ G01
```

两段通信是否同时成立。

只有基础测试通过后，再进行大规模优化。

---

# 29. 最终验收标准

最终项目至少满足：

$$
\boxed{\text{所有原始数据可以自动读取}}
$$

$$
\boxed{\text{不存在人工手填中间结果才能继续运行}}
$$

$$
\boxed{\text{Q1-Q4均可独立运行}}
$$

$$
\boxed{\text{Q1-Q4也可顺序一键运行}}
$$

$$
\boxed{\text{所有输出方案均通过独立validator}}
$$

$$
\boxed{\text{所有货箱恰好配送一次}}
$$

$$
\boxed{\text{所有硬约束满足}}
$$

$$
\boxed{\text{Q3通信中断时长为0}}
$$

$$
\boxed{\text{Q4分区满足题目继承关系}}
$$

$$
\boxed{\text{结果可以导出为Excel}}
$$

$$
\boxed{\text{关键结果有可视化}}
$$

$$
\boxed{\text{README可使其他人独立复现实验}}
$$

---

# 30. 最重要的开发原则

请严格遵守：

> **先读原始题目，再读全部相关附件，再实现模型。不要把本任务书当作原题公式的替代品。**

> **任何题目未明确规定的约束，不要自行加入。**

> **任何原题明确存在的规则，不要为了简化优化而静默删除。**

> **如果采用简化、近似、离散化或启发式算法，必须明确记录简化方式，并最终使用原始物理模型重新验证。**

> **优化算法负责找方案，独立验证器负责判断方案是否合法。**

> **代码的目标不仅是得到一个数值结果，还应能够支撑数学建模论文中的模型描述、结果表格、敏感性分析和图形展示。**

---

## 我还建议你在最开头再加一句给 Codex

这一句很有用：

> **不要急于一次性完成全部代码。首先阅读工作区中的题目 DOCX、所有 Excel、地理空间说明 PDF 和结果提交模板，完成数据结构、公式、约束和输出要求审计，并先向我汇报你识别出的模型规则、文件字段、可能歧义和拟采用的工程结构；确认基础模块无误后再逐问实现。**


